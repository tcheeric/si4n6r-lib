<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>nostr.si4n6r</groupId>
    <artifactId>si4n6r-lib</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <modules>
        <module>si4n6r-signer</module>
        <module>si4n6r-test</module>
        <module>si4n6r-util</module>
        <module>si4n6r-storage</module>
        <module>si4n6r-storage-fs</module>
        <module>si4n6r-jpa</module>
        <module>si4n6r-rest</module>
    </modules>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.11.0</version>
                    <configuration>
                        <showDeprecation>true</showDeprecation>
                        <annotationProcessorPaths>
                            <annotationProcessorPath>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </annotationProcessorPath>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.1.2</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>3.1.2</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>integration-test</goal>
                                <goal>verify</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>flatten-maven-plugin</artifactId>
                    <version>1.5.0</version>
                    <configuration>
                        <updatePomFile>true</updatePomFile>
                        <flattenMode>resolveCiFriendliesOnly</flattenMode>
                    </configuration>
                    <executions>
                        <execution>
                            <id>flatten</id>
                            <phase>process-resources</phase>
                            <goals>
                                <goal>flatten</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>flatten.clean</id>
                            <phase>clean</phase>
                            <goals>
                                <goal>clean</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <properties>
        <lombok.version>1.18.24</lombok.version>
        <bouncy-castle.version>1.75</bouncy-castle.version>
        <java-jwt.version>4.4.0</java-jwt.version>
        <nostr-java.version>0.4-SNAPSHOT</nostr-java.version>

        <!-- Test Dependency Versions -->
        <junit.version>5.9.1</junit.version>
        <assertj.version>3.23.1</assertj.version>

        <lombok.version>1.18.24</lombok.version>

        <maven-compiler-plugin.version>3.11.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.1.2</maven-surefire-plugin.version>
        <maven-failsafe-plugin.version>3.1.2</maven-failsafe-plugin.version>
        <flatten-maven-plugin.version>1.5.0</flatten-maven-plugin.version>
        <maven-war-plugin.version>3.3.2</maven-war-plugin.version>

        <spring.boot.starter.test.version>3.2.0</spring.boot.starter.test.version>
        <spring.boot.maven.plugin.version>3.2.0</spring.boot.maven.plugin.version>
        <spring.boot.starter.data.jpa.version>3.2.0</spring.boot.starter.data.jpa.version>
        <spring.boot.starter.jdbc.version>3.2.0</spring.boot.starter.jdbc.version>
        <spring.boot.starter.validation.version>3.2.0</spring.boot.starter.validation.version>
        <spring.boot.starter.data.rest.version>3.2.0</spring.boot.starter.data.rest.version>
        <spring.boot.starter.web.version>3.2.0</spring.boot.starter.web.version>
        <spring.boot.starter.test.version>3.2.0</spring.boot.starter.test.version>

        <hibernate.version>6.2.4.Final</hibernate.version>
        <jaxb.version>4.0.2</jaxb.version>
        <mapstruct.version>1.4.2.Final</mapstruct.version>

        <postgresql.version>42.3.1</postgresql.version>

        <logback.version>1.4.11</logback.version>
        <lombok.version>1.18.24</lombok.version>
        <jakarta.servlet-api.version>6.0.0</jakarta.servlet-api.version>
        <jackson.databind.version>2.15.1</jackson.databind.version>
        <junit.version>5.9.2</junit.version>
        <maven-war-plugin.version>3.3.2</maven-war-plugin.version>

        <tomcat7-maven-plugin.version>2.2</tomcat7-maven-plugin.version>

        <jakarta.servlet-api.version>6.0.0</jakarta.servlet-api.version>

        <!-- Test Dependency Versions -->
        <junit.version>5.9.2</junit.version>
        <assertj.version>3.23.1</assertj.version>

        <maven.compiler.source>20</maven.compiler.source>
        <maven.compiler.target>20</maven.compiler.target>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

</project>